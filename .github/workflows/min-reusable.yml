name: Min Reusable
on:
  workflow_call:
    inputs:
      lane:
        required: true
        type: string
permissions:
  contents: read
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "lane=${{ inputs.lane }}"
